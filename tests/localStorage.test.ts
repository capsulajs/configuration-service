import { LocalStorageProvider } from 'src/transport';

describe('Test suite fro the LocalStorageProvider', () => {
  console.log(LocalStorageProvider);
  it('Boilerplate', () => expect(1).toBeTruthy());
});
